import classes from "./SupportService.module.css";

const SupportService = () => {
    return (
        <div className={classes.main__wrapper}>
            support
        </div>
    )
}


export default SupportService;